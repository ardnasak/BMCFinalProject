name: ecommerce_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# ... (version and environment sections remain the same) ...
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # 👇 **FIX: UPDATING ALL FIREBASE PACKAGES FOR COMPATIBILITY**
  firebase_core: ^2.25.5     # Updated from 4.2.0
  firebase_auth: ^4.17.5    # Updated from 6.1.1
  cloud_firestore: ^4.15.5  # Updated version

  flutter_native_splash: 2.4.0
  provider: ^6.1.5+1

dev_dependencies:
  # ... (dev_dependencies section remains the same) ...
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

# ... (rest of the file remains the same) ...
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/donut.jpg
  android_12:
    color: "#FFFFFF"
    image: assets/images/splash_logo.png
web: true